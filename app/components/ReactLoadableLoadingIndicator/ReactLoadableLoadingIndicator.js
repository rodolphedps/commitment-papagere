import React from 'react';

import LoadingIndicator from '../LoadingIndicator';

const ReactLoadableLoadingIndicator = () => (
  <LoadingIndicator color="black" />
);

export default ReactLoadableLoadingIndicator;
